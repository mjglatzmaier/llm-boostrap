transformers>=4.49.0
accelerate>=1.4.0
huggingface_hub>=0.29.0
sentencepiece>=0.2.0
safetensors>=0.5.0
psutil>=5.9.0
pynvml>=11.5.0
# NOTE: PyTorch is installed separately with the correct CUDA index URL.
# See setup.sh or the README for instructions.